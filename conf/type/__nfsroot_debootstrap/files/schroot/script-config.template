#!/bin/sh

cat << DONE
# Generated by cdist __nfsroot_debootstrap
# Changes will be overwritten.

# Nfsroot specific settings for chroot setup and exec scripts.
# See schroot-script-config(5) for further details.

DONE
if [ -n "$fstab" ]; then
   echo "# Filesystems to mount inside the chroot."
   echo "FSTAB=\"/etc/schroot/$fstab\""
fi
if [ -n "$copyfiles" ]; then
   echo "# Files to copy from the host system into the chroot."
   echo "COPYFILES=\"/etc/schroot/$copyfiles\""
fi
