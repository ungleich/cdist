#!/bin/sh
cat << DONE
# Generated by cdist __schroot_chroot
# Changes will be overwritten.
[$name]
DONE
[ -n "$description" ] && echo "description=$description"
[ -n "$type" ] && echo "type=$type"
[ -n "$directory" ] && echo "directory=$directory"
[ -n "$script_config" ] && echo "script-config=$script_config"
if [ "$arch" = "i386" ]; then
   echo "personality=linux32"
fi
